# NOTE: Netlify CMS / Decap CMS has been removed.
# This project now uses a custom admin panel (admin/index.html) with GitHub API integration.
# Product data is managed through the custom panel and deployed via Cloudflare Pages.
